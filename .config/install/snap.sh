# sudo snap install hello-world
# hello-world
# read -p "Press <ENTER> to continue..."

# sudo snap install snapcraft --classic
# sudo snap install multipass --classic

# echo
# echo "Lets loging with 'support@nanocube.co.za'"
# echo "Uses (ub)untu account, nudge nudge"
# sudo snap login

# sudo snap install mir-kiosk

# not sure: sudo apt-add-repository --update ppa:mir-team/release
# not sure: sudo apt install mir-demos mir-graphics-drivers-desktop

# # Download Core image from: https://ubuntu.com/core/docs/supported-platforms
# # Last one "Generic Generic x86 / KVM", this include AMD64/x64
# sudo mkdir -p ~/images/
# echo "Downloading core image for generic AMD64..."
# curl http://cdimage.ubuntu.com/ubuntu-core/20/stable/current/ubuntu-core-20-amd64.img.xz -o ~/images/ubuntu-core-20-amd64.img.xz
# echo "Download complete, extracting:"
# unxz ~/images/ubuntu-core-20-amd64.img.xz
# echo "COMPLETE."
# Install 'Startup Disk Creator', the icon looks like a harddrive with ubuntu orange symbol (not sure about the flash stick looking one)
# And follow this guide: https://ubuntu.com/tutorials/create-a-usb-stick-on-ubuntu#3-launch-startup-disk-creator
# Insert into target PC, reboot, then smash 'F11' to bring up the boot menu.
